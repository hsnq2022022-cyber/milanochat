# Milano Widgets - محرر متقدم

## 📋 ملخص التحديثات

تم تطوير صفحة `/#/widgets` من محرر بدائي بأربعة حقول إلى محرر احترافي كامل بمستوى Intercom/Crisp/Tawk.to.

## 🎯 الميزات المنفذة

### 1. بنية المحرر ✅
- **لوحة جانبية بتبويبات**: عام / المظهر / المحادثة / السلوك / النماذج / التثبيت
- **معاينة مباشرة (Live Preview)** تتحدث فوراً مع كل تغيير
- **أزرار تبديل**: سطح المكتب / جوال، فاتح / داكن، حالة مغلق / مفتوح
- **شريط علوي ثابت**: حفظ، إلغاء، تراجع/إعادة (Undo/Redo)

### 2. المظهر ✅
- **قوالب جاهزة (Presets)**: Minimal, Modern, Glass, Dark Pro, Rounded
- **اللون**: منتقي ألوان كامل (HEX) + تحقق من الصحة
- **الخطوط**: اختيار الخط (Cairo, Tajawal, IBM Plex Arabic, System)
- **الشكل**: نصف قطر الزوايا (Slider)، الظل (بدون/خفيف/متوسط/قوي)
- **زر الإطلاق (Launcher)**: اختيار أيقونة، حجمه، شكله
- **الموقع**: يمين/يسار
- **صورة رمزية (Avatar)** للوكيل + اسم الوكيل + وصف + مؤشر الحالة

### 3. المحادثة والسلوك ✅
- **رسالة ترحيب** + Placeholder
- **مؤشر الكتابة** + إيصالات القراءة
- **الفتح التلقائي**: معطّل / بعد تأخير / عند نية الخروج / عند التمرير
- **صوت تنبيه** عند وصول رسالة
- **إخفاء على الجوال** اختيارياً

### 4. النماذج ✅
- **نموذج ما قبل المحادثة (Pre-chat)**: قابل للتفعيل
- **نموذج ترك رسالة (Offline Form)**: قابل للتفعيل

### 5. التثبيت ✅
- **كود التضمين** جاهز مع زر نسخ
- **تعليمات** لـ WordPress / Shopify / React

### 6. جودة التنفيذ ✅
- **تحقق فوري** من الحقول مع رسائل خطأ واضحة
- **حفظ تلقائي** مع مؤشر "تم الحفظ"
- **تراجع/إعادة** (Undo/Redo)
- **انتقالات ناعمة** 150-250ms
- **تصميم متجاوب** بالكامل

## 📁 الملفات المُنشأة/المُعدّلة

### ملفات جديدة:
1. **`src/types/widget.ts`** - أنواع TypeScript شاملة للمحرر المتقدم
2. **`src/components/WidgetPreview.tsx`** - معاينة حية متقدمة
3. **`src/components/WidgetEditor.tsx`** - المحرر الكامل بتبويبات
4. **`supabase/widgets_settings_migration.sql`** - تحديث Schema

### ملفات مُعدّلة:
1. **`src/pages/Widgets.tsx`** - تم إعادة كتابته بالكامل ليستخدم المحرر الجديد
2. **`src/components/Icons.tsx`** - إضافة 4 أيقونات جديدة (Minimize, Maximize, Undo, Redo)
3. **`server/src/routes/widgets.ts`** - دعم settings كـ JSONB

## 🗄️ تحديث قاعدة البيانات

**مهم**: يجب تنفيذ ملف `supabase/widgets_settings_migration.sql` في Supabase SQL Editor:

```sql
ALTER TABLE public.widgets 
ADD COLUMN IF NOT EXISTS settings JSONB DEFAULT '{}'::jsonb;

CREATE INDEX IF NOT EXISTS widgets_settings_idx 
ON public.widgets USING GIN (settings);
```

هذا يضيف عمود `settings` كـ JSONB لتخزين الإعدادات الموسعة.

## 🚀 الخطوات التالية

### 1. تنفيذ Migration
```bash
# في Supabase SQL Editor:
# نفّذ supabase/widgets_settings_migration.sql
```

### 2. رفع التعديلات
```bash
git add .
git commit -m "تطوير محرر Widgets المتقدم"
git push origin main
```

### 3. إعادة النشر
- Railway سيعيد النشر تلقائياً
- أعد بناء الواجهة: `npm run build && npx gh-pages -d dist`

## 🧪 اختبار المحرر

1. افتح `https://hsnq2022022-cyber.github.io/milanochat/#/widgets`
2. اضغط **"إنشاء Widget"**
3. جرّب التبويبات المختلفة:
   - **عام**: غيّر الاسم واللغة
   - **المظهر**: جرّب القوالب الجاهزة، غيّر اللون والخط
   - **المحادثة**: غيّر رسالة الترحيب
   - **السلوك**: فعّل الفتح التلقائي
   - **النماذج**: فعّل نموذج ما قبل المحادثة
   - **التثبيت**: انسخ كود التضمين
4. لاحظ أن المعاينة تتحدث فوراً مع كل تغيير
5. اضغط **"حفظ"**
6. افتح Widget في صفحة HTML اختبارية

## 🎨 الميزات المتبقية (اختيارية)

- [ ] دعم متعدد اللغات للنصوص (عربي/إنجليزي)
- [ ] Quick Replies قابلة للإضافة والحذف والترتيب
- [ ] أوقات العمل (جدول أسبوعي + المنطقة الزمنية)
- [ ] قواعد الظهور (إظهار/إخفاء في صفحات محددة)
- [ ] رفع ملفات (مع تحديد الأنواع والحجم الأقصى)
- [ ] فحص تباين WCAG AA تلقائي
- [ ] تدرّج لوني (Gradient) للهيدر
- [ ] شارة إشعار (Badge) على Launcher
- [ ] إزاحة أفقية وعمودية بالبكسل (Offset X/Y)

## 📊 نتيجة البناء

```
✓ 201 modules transformed
✓ built in 5.36s

dist/index.html                      1.24 kB
dist/assets/index-B88NJ3mu.css      71.07 kB
dist/assets/Widgets-DVtTCs5G.js     29.93 kB  ← المحرر المتقدم
dist/assets/Dashboard-BnsUVdSn.js   58.56 kB
dist/assets/supabase-t8q6ozDF.js   223.03 kB
dist/assets/index-D_V5UyQw.js      407.49 kB
```

**البناء ناجح بدون أي أخطاء** ✅

## 🔒 الأمان

- ✅ التحقق من صحة HEX color
- ✅ التحقق من وجود اسم الـ Widget
- ✅ Rate Limiting على جميع الـ endpoints
- ✅ RLS Policies لعزل البيانات بين الـ tenants
- ✅ لا مفاتيح سرية في الـ Frontend

## 🎉 الخلاصة

تم تطوير محرر Widgets من مستوى بدائي إلى مستوى احترافي كامل مع:
- ✅ بنية تبويبات منظمة
- ✅ معاينة حية متقدمة
- ✅ قوالب جاهزة
- ✅ تخصيص كامل للمظهر
- ✅ إعدادات السلوك والنماذج
- ✅ كود تضمين جاهز
- ✅ حفظ تلقائي + تراجع/إعادة
- ✅ تصميم متجاوب

**المحرر جاهز للاستخدام الفعلي** 🚀
