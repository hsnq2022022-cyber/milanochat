# Human Agent Demo Mode - دليل الاستخدام

## نظرة عامة

تم إضافة وضع **Human Agent Demo** لعرض تجربة تحويل المحادثة من الذكاء الاصطناعي إلى موظف بشري أمام Meta App Review.

## الوصول إلى Demo

### الطريقة 1: من شريط التنقل
- افتح الموقع الرئيسي
- اضغط على زر **"👨‍💼 Human Agent Demo"** في شريط التنقل العلوي

### الطريقة 2: عبر الرابط المباشر
- انتقل إلى: `https://your-domain.com/#/demo`

## ما الذي تم تعطيله؟

### 1. زر "اربط واتساب" في الصفحة الرئيسية
- **الموقع**: `src/components/Hero.tsx` (السطر ~1012)
- **الحالة**: معطل (معلق بتعليق)
- **السبب**: عرض تجربة Meta App Review بدون ربط WhatsApp فعلي

### 2. مرحلة عرض QR Code
- **الموقع**: `src/components/Hero.tsx` (السطر ~1053)
- **الحالة**: معطل (معلق بتعليق)
- **السبب**: عدم عرض QR Code للربط

### 3. نافذة QrModal في لوحة التحكم
- **الموقع**: `src/pages/Dashboard.tsx` (السطر ~1346)
- **الحالة**: معطل (معلق بتعليق)
- **السبب**: عدم عرض نافذة ربط WhatsApp

## ما الذي تم إضافته؟

### 1. صفحة Human Agent Demo
- **الموقع**: `src/pages/HumanAgentDemo.tsx`
- **الوصول**: `/#/demo`
- **المحتوى**:
  - محادثة تجريبية بين عميل و AI Agent
  - زر تحويل إلى موظف بشري
  - واجهة كتابة ردود كموظف بشري
  - شرح السيناريو والميزات

### 2. مكون HumanAgentChat
- **الموقع**: `src/components/HumanAgentChat.tsx`
- **الوظيفة**: عرض المحادثة التجريبية
- **الميزات**:
  - رسائل ملونة حسب المرسل (عميل/AI/موظف بشري)
  - مؤشرات كتابة حية
  - زر تحويل إلى موظف بشري
  - حقل إدخال للرد كموظف بشري

### 3. إعدادات Demo
- **الموقع**: `src/lib/demoConfig.ts`
- **المحتوى**:
  - بيانات العميل الافتراضي
  - بيانات AI Agent
  - بيانات الموظف البشري
  - سيناريو المحادثة الافتراضي

## كيفية إعادة التفعيل

### لإعادة تفعيل زر "اربط واتساب":
1. افتح `src/components/Hero.tsx`
2. ابحث عن السطر ~1012
3. أزل علامات التعليق `{/* */}` حول الزر

### لإعادة تفعيل مرحلة QR Code:
1. افتح `src/components/Hero.tsx`
2. ابحث عن السطر ~1053
3. أزل علامات التعليق `{/* */}` حول المرحلة

### لإعادة تفعيل QrModal:
1. افتح `src/pages/Dashboard.tsx`
2. ابحث عن السطر ~1346
3. أزل علامات التعليق `{/* */}` حول QrModal

## السيناريو المعروض

### المرحلة 1: محادثة مع AI Agent
```
العميل: السلام عليكم، أريد معرفة تفاصيل المنتج.
AI: وعليكم السلام، أهلاً بك أحمد. بالتأكيد، سأساعدك في معرفة التفاصيل.
```

### المرحلة 2: طلب تحويل إلى موظف بشري
```
العميل: أريد التحدث مع موظف.
[يظهر زر "تحويل إلى موظف بشري"]
```

### المرحلة 3: التحويل
```
[بعد الضغط على الزر]
النظام: جاري تحويلك إلى موظف خدمة العملاء...
النظام: المحادثة الآن مع الموظف البشري
```

### المرحلة 4: محادثة مع موظف بشري
```
[يمكن للموظف البشري كتابة ردود من لوحة التحكم]
الموظف: أهلاً بك، كيف يمكنني مساعدتك؟
```

## الملفات المعدلة

### ملفات جديدة:
1. `src/pages/HumanAgentDemo.tsx` - صفحة العرض
2. `src/components/HumanAgentChat.tsx` - مكون المحادثة
3. `src/lib/demoConfig.ts` - إعدادات Demo

### ملفات معدلة:
1. `src/App.tsx` - إضافة مسار `#/demo`
2. `src/components/Hero.tsx` - تعطيل زر QR ومرحلة الربط
3. `src/pages/Dashboard.tsx` - تعطيل QrModal
4. `src/components/Navbar.tsx` - إضافة رابط للصفحة الجديدة

## ملاحظات مهمة

### ⚠️ هذه محادثة تجريبية
- **ليست متصلة بـ WhatsApp فعلي**
- **ليست متصلة بـ Meta فعلي**
- **مخصصة للعرض التوضيحي فقط**

### ✅ ما يتم عرضه
- تجربة تحويل المحادثة من AI إلى موظف بشري
- واجهة مستخدم احترافية
- سيناريو واقعي

### ❌ ما لا يتم عرضه
- ربط WhatsApp حقيقي
- تكامل مع Meta API
- رسائل حقيقية

## أوامر npm

### للبناء والتشغيل:
```bash
# تثبيت الاعتمادات
npm install

# تشغيل في وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# معاينة البناء
npm run preview
```

## التراجع الكامل

إذا أردت العودة إلى الحالة السابقة (قبل إضافة Demo Mode):

```bash
# 1. التراجع عن التعديلات
git checkout src/components/Hero.tsx
git checkout src/pages/Dashboard.tsx

# 2. حذف الملفات الجديدة
rm src/pages/HumanAgentDemo.tsx
rm src/components/HumanAgentChat.tsx
rm src/lib/demoConfig.ts

# 3. التراجع عن App.tsx و Navbar.tsx
git checkout src/App.tsx
git checkout src/components/Navbar.tsx

# 4. إعادة البناء
npm run build
```

## الدعم

للأسئلة أو المشاكل، يرجى فتح issue في المستودع.

---

**آخر تحديث**: 2026
**الإصدار**: 1.0.0
